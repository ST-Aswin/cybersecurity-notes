# Dashed File
    The filename `-` is interpreted by many commands as an option.
    By prefixing it with `./`, we provide an explicit path (`./-`), 
    allowing the shell and the command to treat it as a filename 
    instead of an option.
